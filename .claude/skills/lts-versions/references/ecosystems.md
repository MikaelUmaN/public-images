# Ecosystems

One row per ecosystem or package the repository installs: where releases are published, the
command that lists them, what "long-term stable" means there, and which coupled group in the
`version-pinner` agent it belongs to.

"gate" in the LTS notion column is the maturity gate of SKILL.md, "Apply the LTS rule".

crates.io requires a User-Agent that identifies the caller:

```
curl -fsSL -A 'public-images-version-pinner (https://github.com/mikaeluman/public-images)' "https://crates.io/api/v1/crates/<crate>/versions"
```

The local secret-file guard blocks any Bash command whose text contains `.key`. jq filters over
`to_entries` therefore index the pair, `[.[]] | select(.[0] | test("…"))`, instead of naming the
field.

| Ecosystem / package | Source of truth | List versions | LTS notion | Coupling |
|---|---|---|---|---|
| GitHub releases (generic) | the project's releases page | `gh api "repos/<o>/<r>/releases?per_page=30" --jq '.[] \| select(.prerelease==false and .draft==false) \| "\(.tag_name)\t\(.published_at)"'` | gate | per project |
| Rust | `https://releases.rs`, `https://static.rust-lang.org/dist/channel-rust-stable.toml` | `curl -fsSL https://static.rust-lang.org/dist/channel-rust-stable.toml \| grep -m1 -A2 '^\[pkg.rust\]'`; `gh api "repos/rust-lang/rust/releases?per_page=10" --jq '.[] \| "\(.tag_name)\t\(.published_at)"'` | latest stable whose x.y.0 is at least 14 days old | G4: `RUST_VERSION` ≥ every `--locked` crate's MSRV |
| rustup | `https://static.rust-lang.org/rustup/archive/<ver>/x86_64-unknown-linux-gnu/rustup-init` and `rustup-init.sha256` (format `<hash> *./rustup-init`); `rust-lang/rustup` publishes tags, no GitHub releases | `gh api "repos/rust-lang/rustup/tags?per_page=5" --jq '.[].name'` | gate | `RUSTUP_VERSION` in `rust-datascience.docker`; independent of `RUST_VERSION` |
| crates.io | `https://crates.io/api/v1/crates/<crate>/versions` | `curl -fsSL -A '<ua>' "https://crates.io/api/v1/crates/<crate>/versions" \| jq -r '.versions[] \| select(.yanked==false) \| "\(.num)\t\(.created_at)\t\(.rust_version)"'`; a null `rust_version` (evcxr, cargo-deps, cargo-sweep, cargo-bloat, cargo-machete, cargo-generate) is proven by the current build for the installed version and falls back to `gh api "repos/<o>/<r>/contents/Cargo.toml?ref=<tag>" --jq .content \| base64 -d \| grep rust-version` for a new candidate; one request per second | gate | MSRV vs `RUST_VERSION`; `nu_plugin_polars` version == Nushell version |
| Nushell | `https://github.com/nushell/nushell/releases` | generic GitHub command with `nushell/nushell` | latest 0.x.y at least 14 days old whose `nu_plugin_polars` exists on crates.io | G1 |
| DuckDB | `https://github.com/duckdb/duckdb/releases`, `https://duckdb.org/data/duckdb-releases.csv` (columns `release_date,version_number,lts,…,end_of_life`), `https://duckdb.org/release_calendar` for planned dates | generic GitHub command with `duckdb/duckdb`; `curl -fsSL https://duckdb.org/data/duckdb-releases.csv` | the `lts=true` line with the highest version number, highest patch; expiring (`end_of_life` within six months) advances to the newest line, LTS or not, and the CSV lists no successor before it is released | G2: CLI == libduckdb == `libduckdb-sys` (crates.io versions `1.4.x` mirror DuckDB 1.4.x; `1.105xx.0` mirror 1.5.x) |
| kubectl | `https://dl.k8s.io/release/stable.txt`, `https://dl.k8s.io/release/stable-<minor>.txt`, `https://endoflife.date/api/kubernetes.json` | `curl -fsSL https://dl.k8s.io/release/stable.txt`; `curl -fsSL https://dl.k8s.io/release/stable-<minor>.txt`; `curl -fsSL https://endoflife.date/api/kubernetes.json \| jq -r '.[:4][] \| "\(.cycle)\t\(.latest)\t\(.releaseDate)\t\(.eol)"'` | highest patch within the minor; a minor move needs `cluster-minor`; the report names the minor's `eol` | G5: ±1 minor against the cluster |
| Helm, k9s | `https://github.com/helm/helm/releases` (3.x and 4.x are released in parallel), `https://github.com/derailed/k9s/releases`, `https://helm.sh/docs/topics/version_skew/` | generic GitHub command with `helm/helm` (`per_page=60` reaches the older major's patches) and `derailed/k9s` | gate; helm stays inside its current major unless the user picks the other | G5: helm supports Kubernetes n..n-3 |
| AWS CLI v2 | `https://github.com/aws/aws-cli/tags` (the releases page holds one 2018 dev prerelease and nothing else), `CHANGELOG.rst` on branch `v2` | `gh api "repos/aws/aws-cli/tags?per_page=8" --jq '.[].name'`; date of a tag `gh api repos/aws/aws-cli/commits/<tag> --jq .commit.committer.date` | latest 2.x whose 2.N.0 is at least 7 days old; releases land almost daily | none |
| uv | `https://github.com/astral-sh/uv/releases` | generic GitHub command with `astral-sh/uv`; line age `gh api repos/astral-sh/uv/releases/tags/0.N.0 --jq .published_at` | highest patch of the latest 0.N line whose 0.N.0 is at least 7 days old | installer URL `https://astral.sh/uv/<ver>/install.sh` |
| Bun | `https://github.com/oven-sh/bun/releases` (tags `bun-vX.Y.Z`) | generic GitHub command with `oven-sh/bun` | gate; the tracker carries open Linux regressions titled with most releases, judged by whether the image's use (playwright, codex, dotenvx, `node` shim) is in the affected path | installer `bash -s "bun-v<ver>"`; npm `engines` of the globals; G9 |
| npm | `https://registry.npmjs.org/<pkg>` (scoped names URL-encoded, `@openai%2Fcodex`) | `curl -fsSL "https://registry.npmjs.org/<pkg>" \| jq -r '."dist-tags".latest as $l \| "\($l)\t\(.time[$l])\t\(.versions[$l].engines\|tostring)\t\(.versions[$l].deprecated // "no")"'`; history `jq -r '.time \| to_entries[] \| [.[]] \| select(.[0] \| test("^[0-9]+\\.[0-9]+\\.[0-9]+$")) \| @tsv' \| tail -8` | gate on the `latest` tag; codex ships several patches a week and every release has version-titled open issues, so the highest patch of the current minor is the pick | G9: npm playwright major.minor == PyPI playwright major.minor |
| PyPI | `https://pypi.org/pypi/<pkg>/json` | `curl -fsSL "https://pypi.org/pypi/<pkg>/json" \| jq -r '.info.version as $v \| "\($v)\t\(.releases[$v][0].upload_time)\t\(.info.requires_python)\tyanked=\(.releases[$v][0].yanked)"'`; wheels via `.urls[].filename` | gate; yanked excluded | `requires_python`; `cp313` wheels |
| Python | `https://endoflife.date/api/python.json`; in the image `uv python list --all-versions` | `curl -fsSL https://endoflife.date/api/python.json \| jq -r '.[] \| "\(.cycle)\t\(.latest)\t\(.eol)"'` | minor with at least twelve months of support and wheels for torch, numba, pymc (no `cp314` torch wheel exists as of 2026-09, so 3.13 stays) | G6 |
| PyTorch index | `https://download.pytorch.org/whl/cpu`, `https://download.pytorch.org/whl/cu124` | `curl -fsSL https://download.pytorch.org/whl/cpu/torch/ \| grep -o 'torch-[0-9.]*+cpu-cp313[^"]*linux_x86_64[^"]*' \| sort -uV \| tail -1` | Tier C, report only | cu124 vs host driver |
| .NET SDK | `https://builds.dotnet.microsoft.com/dotnet/release-metadata/releases-index.json` | `curl -fsSL https://builds.dotnet.microsoft.com/dotnet/release-metadata/releases-index.json \| jq -r '."releases-index"[] \| "\(."channel-version")\t\(."latest-sdk")\t\(."release-type")\t\(."support-phase")\t\(."eol-date")"'` | `release-type: lts` and `support-phase: active`; a `maintenance` LTS (8.0, eol 2026-11-10) is reported as leaving support | G8 |
| NuGet | `https://api.nuget.org/v3-flatcontainer/<id>/index.json` (versions, no dates); `https://api.nuget.org/v3/registration5-gz-semver2/<id>/<version>.json` for `published` and deprecation, served gzip-compressed so curl needs `--compressed` | `curl -fsSL "https://api.nuget.org/v3-flatcontainer/<id>/index.json" \| jq -r '.versions[] \| select(test("-") \| not)' \| tail -10`; `curl -fsSL --compressed "https://api.nuget.org/v3/registration5-gz-semver2/<id>/<version>.json" \| jq -r .published` (ids lower-case) | gate; pre-release excluded | G8: a tool that installs under both SDKs today proves its target framework |
| Quarto | `https://quarto.org/docs/download/_download.json`; `https://github.com/quarto-dev/quarto-cli/releases` marks every prerelease, so the newest `prerelease==false` tag is the stable channel | `curl -fsSL https://quarto.org/docs/download/_download.json \| jq -r '.version, .published'` | stable channel only | G7 |
| Pico SDK, picotool | `https://github.com/raspberrypi/pico-sdk/releases`, `https://github.com/raspberrypi/picotool/releases` | generic GitHub command with `raspberrypi/pico-sdk` and `raspberrypi/picotool` | highest tag present in both, at least 14 days old; SDK issues titled with the version describe firmware behaviour changes, reported and not blocking the toolchain | G3: identical tags |
| Ubuntu apt (Tier B) | `https://launchpad.net/ubuntu/+source/<pkg>`, `https://endoflife.date/api/ubuntu.json` | `docker run --rm ubuntu:24.04 bash -c 'apt-get update -qq && apt-cache policy <pkg>'`, or `<tool> --version` in the built image | unpinned; the resolved version is recorded | none |
| Third-party apt (chrome, glow, claude-code, gh) | the vendor repositories configured in `datascience.docker` | `<tool> --version` in the built image | unpinned; the resolved version is recorded | playwright's expected Chrome major |
| Node | none: `node` is a symlink to `bun` | — | — | `engines.node` (`>=20` playwright, `>=16` codex) is satisfied by bun's Node compatibility |
